//1. Stop the running containers
docker compose down
//2. Rebuild the image
docker compose build --no-cache
//3. Start the updated API
docker compose up -d --build


Optional: Remove old images (if needed)
docker image prune -f
